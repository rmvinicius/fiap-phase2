apiVersion: v1
kind: Service
metadata:
  name: flag-service-db-svc
  namespace: postgres
spec:
  type: NodePort
  selector:
    app: flag-service-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30440