apiVersion: v1
kind: PersistentVolume
metadata:
  name: flag-service-db-pv
spec:
  storageClassName: local-storage
  claimRef:
    name: flag-service-db-pvc
    namespace: postgres
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/volumes/flags_db"
    type: DirectoryOrCreate