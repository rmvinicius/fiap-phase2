apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flag-service-db-pvc
  namespace: postgres
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi