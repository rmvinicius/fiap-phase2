apiVersion: v1
kind: Service
metadata:
  name: targeting-service-db-svc
  namespace: postgres
spec:
  type: NodePort
  selector:
    app: targeting-service-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30441