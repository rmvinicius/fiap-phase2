apiVersion: v1
kind: Service
metadata:
  name: targeting-service-svc
  namespace: application
spec:
  type: NodePort
  selector:
    app: targeting-service
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
      nodePort: 30003