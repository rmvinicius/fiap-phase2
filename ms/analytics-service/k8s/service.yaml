apiVersion: v1
kind: Service
metadata:
  name: analytics-service-svc
  namespace: application
spec:
  type: NodePort
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8005
      nodePort: 30005